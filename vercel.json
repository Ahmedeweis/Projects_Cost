{
    "builds": [
        {
            "src": "index.html",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "."
            }
        }
    ],
    "build": {
        "env": {
            "BUILD_TIME": "{{now}}"
        }
    }
}